pipeline {
    agent any 
        stages ('basic test') {
            steps {
                sh '''
                echo "hello Champ"
                gcloud version
                echo "Welcome to GCP"
                '''
            }
        }


}
