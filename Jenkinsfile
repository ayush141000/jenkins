pipeline {
    agent any {
        stages {
            steps {
                sh '''
                echo "hello Champ"
                gcloud version
                echo "Welcome to GCP"
                '''
            }
        }

    }
}
